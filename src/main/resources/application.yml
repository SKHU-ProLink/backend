spring:
  application:
    name: SKHU-ProLink

  mongodb:
    uri: ${MONGODB_URI}

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

swagger:
  server-url: ${APP_SERVER_URL}
